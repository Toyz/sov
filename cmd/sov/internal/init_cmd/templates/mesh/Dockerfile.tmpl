# Generated by 'sov init mesh'. Adjust as you wish.
FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
ARG ROLE=gateway
RUN CGO_ENABLED=0 go build -o /out/{{.Project}} ./${ROLE}

FROM gcr.io/distroless/static-debian12
COPY --from=build /out/{{.Project}} /{{.Project}}
ENTRYPOINT ["/{{.Project}}"]
